/** Re-exported so the existing `@/components/dashboard/*` imports keep working;
 *  the mark itself is defined once in `@/components/paperboat-mark`. */
export { PaperboatMark } from "@/components/paperboat-mark";
