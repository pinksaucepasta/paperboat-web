# Repository-local API fixtures

These fixtures and schemas are inputs to the web API tests. Update them with affected
client/server behavior.
Run `node --test dashboard/tests/contracts/preview-tunnel-v1.test.mjs` from this repository.
